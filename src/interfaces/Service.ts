export interface IService {
    id: number;
    name: string;
    descriptionMarkdown: string;
    descriptionHTML: string;
    image: string;
    priceId: string;
    createdAt: string;
    updatedAt: string;
}

